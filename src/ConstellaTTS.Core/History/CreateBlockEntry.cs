// Removed — merged into CreateBlockAction in ConstellaTTS.Core.Actions,
// which is now both IAction and IReversible. The standalone history-entry
// class was a redundant layer; ActionBase's own doc comment notes that
// actions can opt into IReversible to record themselves in history.
// Safe to git-rm.

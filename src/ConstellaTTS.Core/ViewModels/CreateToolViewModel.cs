// Removed — Create tool state lives on IToolModeService (Tool +
// CreateType). No separate Create tool VM is needed since the Section/
// Stage sub-state is a single enum on the shared service. Safe to git-rm.

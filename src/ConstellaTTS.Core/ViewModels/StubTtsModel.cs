// Removed — StubTtsModel existed only as a type parameter for the (now
// deleted) generic SectionViewModel<TModel>. With SectionViewModel.Model
// optional and bound at runtime, no placeholder model type is needed.
// Safe to git-rm.

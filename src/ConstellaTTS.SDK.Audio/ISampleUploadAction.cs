// This file is intentionally empty. Delete it from the IDE.
//
// ISampleUploadAction has moved to the main SDK at
// SDK/UI/Actions/ISampleUploadAction.cs — it's a UI/command
// contract, not an audio domain type, so it belongs alongside the
// other action interfaces (IAction, IAsyncAction, IBindable). The
// audio SDK only carries pipeline types (decoders, processors, the
// sample record, the service); commands that ride on top of them
// live in the layer that already owns the action infrastructure.

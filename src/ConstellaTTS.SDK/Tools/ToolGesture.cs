// Removed — design changed from "Ctrl/Ctrl+Shift modifier resolution"
// to "mouse-button disambiguation when CreateType is None", owned by
// the gesture handler in TrackListView (Phase 4). Safe to git-rm.
